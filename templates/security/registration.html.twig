{% extends 'base.html.twig' %}



{% block body %}
    <h1>Inscription Sur le site</h1>
    {{form_start(form)}}
    {{form_widget(form)}}
    <button type="submit">Inscription ! </button>
    {{form_start(form)}}
{% endblock %}
